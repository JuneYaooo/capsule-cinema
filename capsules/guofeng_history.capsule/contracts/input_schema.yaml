fields:
  angle:
    default: 清廉、义理、人物选择或价值反转
    required: false
    type: string
  reference_image:
    description: Optional local reference image path for identity or style only.
    required: false
    type: string
  target_duration:
    default: 55
    maximum: 60
    required: false
    type: number
  topic_or_script:
    description: Historical figure topic, story angle, or full narration script.
    required: true
    type: string
