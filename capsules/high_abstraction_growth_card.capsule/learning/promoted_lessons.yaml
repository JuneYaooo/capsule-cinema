# 经验晋升规则（promote-and-delete）：
# 本文件只存放"尚未晋升"的候选经验。一条经验一旦写入 recipes/*.md 或 quality/rules.yaml，
# 必须在同一次更新中从这里删除——配方正文是唯一事实来源（single home），
# 这里不保存已晋升条目的副本，避免同一规则多处维护后漂移出矛盾。
#
# 截至 capsule version 7：历史上晋升的 19 条经验已全部并入 recipes/*.md 与
# quality/rules.yaml 对应规则，按晋升规则从本文件移除。
lessons: []
